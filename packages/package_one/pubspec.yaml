name: package_one
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: 3.32.8

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0